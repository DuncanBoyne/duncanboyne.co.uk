// Enable prerendering for all pages by default
export const prerender = true;

// Render real HTML at build time (SSR) so crawlers get full content,
// titles, and structured data. Dynamic data still hydrates client-side.
export const ssr = true;
